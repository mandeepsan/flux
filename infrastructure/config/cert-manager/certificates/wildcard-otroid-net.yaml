apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-otroid-net
  namespace: networking
spec:
  secretName: wildcard-otroid-net-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - otroid.net
    - "*.otroid.net"