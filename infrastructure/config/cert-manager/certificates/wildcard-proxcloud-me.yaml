apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-proxcloud-me
  namespace: networking
spec:
  secretName: wildcard-proxcloud-me-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - proxcloud.me
    - "*.proxcloud.me"