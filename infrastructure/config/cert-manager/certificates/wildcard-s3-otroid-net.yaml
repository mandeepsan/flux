apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-s3-otroid-net
  namespace: networking
spec:
  secretName: wildcard-s3-otroid-net-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - "*.s3.otroid.net"
