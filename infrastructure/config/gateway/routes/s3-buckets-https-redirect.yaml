apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: s3-buckets-https-redirect
  namespace: networking
spec:
  parentRefs:
  - name: otroid-gateway
    sectionName: http-wildcard-s3-otroid-net
  hostnames:
  - "*.s3.otroid.net"
  rules:
  - filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: https
        statusCode: 301
