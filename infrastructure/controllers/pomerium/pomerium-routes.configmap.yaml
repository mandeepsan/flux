apiVersion: v1
kind: ConfigMap
metadata:
  name: pomerium-routes
  namespace: networking
data:
  values.yaml: |
    config:
      routes:
      - from: https://hubble.otroid.net
        to: http://hubble-ui.kube-system.svc.cluster.local
        policy:
        - allow:
            or:
            - claim/groups: devops
