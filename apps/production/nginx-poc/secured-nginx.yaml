apiVersion: apps/v1
kind: Deployment
metadata:
  name: secured-nginx
  namespace: apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: secured-nginx
  template:
    metadata:
      labels:
        app.kubernetes.io/name: secured-nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.29-alpine
        ports:
        - name: http
          containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: secured-nginx
  namespace: apps
spec:
  selector:
    app.kubernetes.io/name: secured-nginx
  ports:
  - name: http
    port: 80
    targetPort: http
